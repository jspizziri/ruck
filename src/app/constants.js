/**
 * Define all app constants
 */
'use strict';

angular.module('ruckApp')
  // make lodash injectable
  .constant('_', window._)
  // make emojione injectable
  .constant('emojione', window.emojione);
